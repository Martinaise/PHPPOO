<?php
echo "PHPOO";
// la difference entre la programation procedurale et la programation objet
// Programmes basés sur les fonctions, l'acces et facilement modifiable
// Programmes basés sur les entités apelees objets, l'acces et complexe

// LA PROGRAMMATION ORIENTE OBJET ET UNE AUTRE ORGANISATION DE CODE ()
// Un objet et la representation d une chose. tout est objet avec des proriété (attributs),exemple une voiture
// les objet provienne de classes que nous créons nous même et qui sont des instances de ces classes (representation)

// une classe et une entité regroupant des variables (attributs) et des fonctions (methodes) propre a celles-ci
// l objectif d une classe et de creer des instances de ces classes (des objet qu'ont pour but de representer des choses) 
// (au finale une classe models de donneés,une structure commune a tout les objetS c'est une sorte de moule a gateau) 
// quand ont cree une insctance d une classe, on cree un objet qui est une representation de la classe

// LES 3 GRAND CONCEPTES
//1- ENCAPSULATION : c'est la protection des donnees d'un objet
//2- HERITAGE : c'est la creation de classes qui heritent des classes existantes
//3- POLYMORPHISM : c'est la creation de classes qui heritent des classes existantes

// EXEMPLE nous voulont cree un espace membre nous alos donc cree une classe qui represente un membre avec les proprietes suivantes
// -nom
// -prenom
// -age
// -email

// et les methodes suivantes

// seConnecter()
// ecrirMessage()
// ecrirCommentaire()
// VOICI MON MOULE A GATEAU


// avec ce moul je vais pouvoir faire des gateau de diferent gouts mais ayant la meme structure
//dans ce cas precis nous alons cree des utilisateurs differents avec la meme architecture


// LA PROGRAMMATION ORIENTE OBJET ET UNE METHODE DE PROGRAMMATION LA PLUS PERFORMANTE
?>